package homework2;

public class PersonalCare extends Product {

    // fields
    private Boolean safety;

    // constructor

    public PersonalCare(String name, String description, Double weight, Double price, Boolean safety) {
        super(name, description, weight, price);
        this.safety = safety;
    }

    // implementing methods
    @Override
    public Double calculatePrice() {
        return calculatePrice();
    }
}
